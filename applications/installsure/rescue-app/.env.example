# InstallSure Rescue App Configuration

# Server port (default: 3000)
PORT=3000

# Optional: Set PG_URL to use Neon Postgres instead of local JSON
# Example: PG_URL=postgres://user:password@host:5432/database
# If not set, tags will be stored in tags.local.json
# PG_URL=
